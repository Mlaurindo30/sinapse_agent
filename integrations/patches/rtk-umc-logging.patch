diff --git a/hooks/hermes/rtk-rewrite/__init__.py b/hooks/hermes/rtk-rewrite/__init__.py
index 6dcf44e..0578464 100644
--- a/hooks/hermes/rtk-rewrite/__init__.py
+++ b/hooks/hermes/rtk-rewrite/__init__.py
@@ -73,0 +74,5 @@ def _pre_tool_call(tool_name=None, args=None, **_kwargs):
+            _log_to_umc(
+                title=f"RTK Opt: {command[:50]}",
+                content=f"Original: {command}\nRewritten: {rewritten}",
+                kind="execution"
+            )
@@ -78,0 +84,21 @@ def _pre_tool_call(tool_name=None, args=None, **_kwargs):
+def _log_to_umc(title, content, kind="execution"):
+    """Saves log to UMC via standalone script to avoid circular dependencies."""
+    try:
+        import os
+        import subprocess
+        from pathlib import Path
+        
+        # Localize sinapse-write.py
+        sinapse_home = os.environ.get("SINAPSE_HOME", os.path.expanduser("~/Documentos/Projects/Hive-Mind"))
+        write_script = os.path.join(sinapse_home, "scripts", "sinapse-write.py")
+        
+        if os.path.exists(write_script):
+            subprocess.Popen(
+                [sys.executable, write_script, "observation", "--title", title, "--content", content, "--kind", kind],
+                stdout=subprocess.DEVNULL,
+                stderr=subprocess.DEVNULL
+            )
+    except:
+        pass
+
+
